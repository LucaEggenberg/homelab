apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homepage-ingressroute
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`home.eggenberg.io`)
      services:
        - kind: Service
          name: homepage
          port: 3000