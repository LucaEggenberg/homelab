top-level-job:
  stage: build
  script:
    - echo "Hello world..."

include:
  - local: "pipelines/gitlab/opentofu-cd.yml"
    rules:
      - changes:
        - "infrastructure/*.tf"
        - "infrastructure/modules/**"
        - "infrastructure/data/**"
